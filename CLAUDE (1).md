# CLAUDE.md — دليل الوكيل لمشروع «موكب أمنا الزهراء»

> اقرأ هذا الملف **قبل** تنفيذ أي طلب في هذا المستودع. يحوي كل ما تحتاجه:
> البنية، القرارات المحسومة، القواعد التي لا تُكسر، أوامر التشغيل، ومزالق
> البيئة التي أهدرت وقتاً سابقاً.

---

## ١. المشروع في سطور

تطبيق موبايل لإدارة موكب حسيني: مشتركون ومتبرعون، مبالغ ودفعات، تقارير
قابلة للطباعة، ومنشورات بنمط إنستغرام.

| | |
|---|---|
| **الإطار** | Flutter · معمارية feature-first · Riverpod · go_router |
| **الخلفية** | Supabase (Auth + Postgres + Storage + Realtime + RLS) |
| **التخزين المحلي** | Hive مشفّر AES-256، المفتاح في Keystore/Keychain |
| **المنصّات** | Android (المستهدف الأساسي) · iOS (الكود جاهز، البناء يحتاج macOS) |
| **اللغة** | العربية فقط · RTL · كل النصوص والتعليقات والـcommits بالعربية |
| **المالك** | `aliali121ew-coder` · المستودع `zahraa` |

---

## ٢. أوامر سريعة

```bash
# ⚠️ بعد استنساخ المستودع لأول مرة: اجلب خطوط Amiri و Cairo
bash tools/fetch_fonts.sh          # لينكس و macOS و CI
powershell -ExecutionPolicy Bypass -File tools\fetch_fonts.ps1   # ويندوز

# تشغيل بالبيانات الحقيقية
flutter run \
  --dart-define=SUPABASE_URL=https://nmpcbyoehmghmietzurs.supabase.co \
  --dart-define=SUPABASE_ANON_KEY=sb_publishable_ev4vJ_FdXww-7Yp_ojV5CA_XDJcrTxW

# تشغيل بلا مفاتيح = وضع البيانات التجريبية الكامل (كل الواجهات تعمل)
flutter run

# بناء APK للتوزيع
flutter build apk --release \
  --target-platform android-arm,android-arm64 \
  --obfuscate --split-debug-info=build/symbols \
  --dart-define=SUPABASE_URL=... --dart-define=SUPABASE_ANON_KEY=...

# الفحص والاختبار — شغّلهما قبل أي commit
flutter analyze && flutter test

# معاينة التصميم كصورة PNG بلا جهاز ولا محاكي
flutter test test/design_preview_test.dart --update-goldens
# الناتج: test/goldens/home_dark.png و home_light.png
```

> **معاينة التصميم أداة مهمة:** لا تعدّل الواجهة ثم تسلّم بلا رؤيتها.
> شغّل المعاينة وافحص الصورة الناتجة فعلياً قبل أن تقول «تم».

---

## ٣. البنية المعمارية

```
lib/
├── core/
│   ├── config/      app_config.dart — المفاتيح من --dart-define وأسماء صناديق Hive والبريد الأساسي
│   ├── data/        supabase_repository.dart — الأساس المشترك + CachedResult + arabicError
│   ├── providers/   app_providers.dart — كل مزوّدات Riverpod
│   ├── router/      app_router.dart — StatefulShellRoute بخمسة فروع
│   ├── storage/     hive_service.dart (مشفّر) · settings_store.dart
│   ├── theme/       app_colors.dart · app_theme.dart
│   ├── utils/       formatters.dart — Fmt للمبالغ والتواريخ
│   └── widgets/     glass.dart (AppBackground/GlassCard/GoldBorder) · girih_pattern.dart · app_shell.dart
├── features/<الميزة>/
│   ├── data/            المستودع (Repository)
│   └── presentation/    الشاشات والودجات
└── shared/
    ├── models/      النماذج + enums.dart + permissions.dart
    └── widgets/     stat_cards.dart · contributor_tile.dart · mawkib_logo.dart
supabase/schema.sql   الجداول + RLS + الدوال + المشغّلات
```

**نمط الميزة الجديدة:** `features/<الاسم>/data/<الاسم>_repository.dart` يرث
`SupabaseRepository`، و `features/<الاسم>/presentation/<الاسم>_page.dart`،
ومزوّداتها تُضاف في `core/providers/app_providers.dart`.

---

## ٤. قواعد لا تُكسر

1. **الأمان في السيرفر لا في التطبيق.** أي صلاحية جديدة تُفرض في RLS داخل
   `supabase/schema.sql` **أولاً**، ثم تُعكس في `shared/models/permissions.dart`
   لإخفاء الأزرار. إخفاء زر ليس حماية.

2. **لا تكسر تكافؤ الصلاحيات.** `permissions.dart` مرآة لسياسات RLS. تعديل
   أحدهما بلا الآخر خطأ.

3. **العضو لا يرى الأسماء إطلاقاً.** أرقامه تأتي من دالة `get_stats()` المجمّعة
   لا من الجداول. لا تُضف مساراً يسرّب أسماء لدور `member`.

4. **التبرع العيني لا يدخل أي مجموع نقدي.** لا في `get_stats` ولا في المشغّلات
   ولا في الواجهة. يُعدّ فقط.

5. **حماية من الـoverflow في كل شاشة:** كل رقم كبير داخل `FittedBox`، وكل نص
   متغيّر له `maxLines` و `TextOverflow.ellipsis`. مقياس خط النظام محدود في
   `app.dart` بين 0.9 و 1.25.

6. **الزجاجي بحساب.** `GlassCard(blur: true)` للكروت **الثابتة** فقط ومحدودة
   العدد. داخل أي قائمة متمرّرة استخدم `blur: false` — التمويه يُعاد حسابه لكل
   عنصر في كل إطار ويقتل الـ60fps.

7. **لا تُودع أسراراً.** `android/keys/` و `android/key.properties` مستثناة في
   `.gitignore`. لا ترفع `.jks` ولا كلمة مرور. مفتاح Supabase العلني استثناء
   مقصود — مصمَّم ليكون مكشوفاً، والحماية في RLS.

8. **صندوق `outbox` مقدّس.** يحوي كتابات لم تُرفع. لا تحذفه عند فشل الفتح —
   `hive_service.dart` يرمي الخطأ عمداً بدل ابتلاعه. أي «إصلاح» يحذفه صامتاً
   يُضيّع عمل المستخدم.

9. **لا `build_runner`.** النماذج تُكتب يدوياً بـ `fromJson`/`toJson`، وHive
   يخزّن خرائط JSON بلا TypeAdapter. توليد الكود يفشل بصمت ويعطّل البناء.

10. **العربية في كل شيء:** الواجهة، التعليقات، رسائل الـcommit، أسماء الأقسام.
    الأخطاء التقنية تُترجم عبر `arabicError()` — لا تعرض `42501` للمستخدم.

---

## ٥. نظام التصميم والهوية البصرية

### الألوان الملكية المعتمدة
مستخرجة **قياساً فعلياً** من بكسلات شعار الموكب — لا تخترع ألواناً جديدة:

| اللون | القيمة السداسية | الاستخدام |
|---|---|---|
| ذهبي أساسي | `#C6A77B` (`AppColors.gold`) | الحواف والأيقونات والعناوين البارزة |
| ذهبي ساطع | `#EBD3A8` (`goldBright`) | النصوص والأرقام على الخلفيات الداكنة |
| ذهبي داكن | `#8A6A33` (`goldDark`) | النصوص على الخلفيات الفاتحة |
| برونزي | `#8D7759` · عنابي `#7D493A` · فيروزي `#3D6D78` | لمسات ثانوية |
| سلّم الأخضر | `greenAbyss #04120C` → `green #14512F` → `greenGlow #1B7A4B` | الخلفيات الملكية والبطاقات |
| الحالات | مسدد `#2E9E6B` · متأخر `#C4553F` · معلّق `#D79A3C` | مؤشرات السداد والطلبات |

### الخطوط الرسمية
- **`AppTheme.fontFamily` & `displayFamily` = Cairo**: تم اعتماد خط **Cairo** الموحّد لجميع شاشات وعناوين وأزرار وأرقام التطبيق (مضمنة ملفاته أوفلاين).
- **صفحة المشتركين (`ContributorsPage`):** مصممة بشبكة كروت زجاجية مكونة من **6 كروت مع أيقونات ملونة** (إضافة مشترك، إضافة متبرع، عرض المشتركين، عرض المتبرعين، عرض الكل، الإحصائيات) بحسب النموذج المطلوب.

---

## ٦. قاعدة البيانات والصلاحيات والأدوار

**المشروع:** `nmpcbyoehmghmietzurs` · المخطط في `supabase/schema.sql` (13 جدولاً · 29 سياسة · 11 دالة).

| الجدول | الغرض |
|---|---|
| `profiles` | مرتبط بـ auth.users · `role` + `status` |
| `contributors` | المساهمون · `type` (subscriber/donor) · حقول الاشتراك · حذف ناعم |
| `payments` | كل دفعة على حدة + رقم وصل |
| `donations` | نقدي (بمبلغ) أو عيني (بوصف وكمية، **بلا مبلغ**) + إهداء لمتوفّى |
| `purchases` | المشتريات ومصروفات الموكب وسندات الصرف |
| `posts` + `post_images` | المنشورات وصور الكاروسيل + `year` للأرشيف |
| `post_likes` / `post_comments` / `post_saves` / `follows` | التفاعل الاجتماعي |
| `story_categories` + `stories` | أقسام الستوريز وخلاصة القصص |
| `notifications` | التنبيهات وإشعارات المتأخرين |

### طبقات الوصول والرتب
| الدور | يرى الصلاحيات |
|---|---|
| زائر (`anon`) | المنشورات والستوريز فقط · **يعجب** بلا حساب |
| عضو معتمد (`member`) | الإحصائيات المجمّعة + المنشورات · **بلا أي اسم** · يعلّق ويحفظ |
| ناشر (`publisher`) | + النشر ورفع القصص والستوريز |
| مسؤول مالي (`finance`) | + عرض وطباعة التقارير بالأسماء وسجل المشتريات (**قراءة فقط**) |
| مدير عام (`admin`) | كل شيء + الموافقات وتعيين الأدوار والحظر + التعديل والحذف الشامل |

### إدارة المدير العام
* المدير العام هو أي حساب يحمل `role = admin` و `status = approved` في جدول `profiles` بقاعدة البيانات.
* الحماية عبر سياسات RLS على السيرفر — التطبيق يقرأ الدور فقط ولا يستطيع رفعه.
* عند فقدان حساب المدير: سجّل حساباً جديداً (يظهر معلقاً)، ثم نفّذ سطر SQL الطوارئ (انظر `supabase/secure_admin_fix.sql`).

---

## ٧. سجل القرارات الهندسية والتحديثات الشاملة

1. **إعادة تصميم واجهة تسجيل الدخول وإنشاء الحساب بهوية مريحة للعين وفخمة (`Emerald & White Auth`):**
   - اعتماد درجات الأخضر الزمردي الراقي (`#0F3D27` / `#0A331E`) مع الأبيض النقي واللمسات الذهبية الهادئة والبطاقات الزجاجية الخفيفة، مما يمنع إجهاد العين.
   - إضافة كبسولة تبديل سفلية بلون أبيض مع أخضر زمردي فاخر ومؤشر انزلاقي ناعم للتنقل بين (تسجيل الدخول ↔ إنشاء حساب).
   - تزويد حقل كلمة المرور بمقياس ذكي لقوة كلمة المرور مع اشتراط 8 خانات على الأقل، وحقل تأكيد كلمة المرور مع فحص تطابق فوري وتنبيه حي.
   - حفظ الاسم ورقم الهاتف والبريد في Supabase Auth، وتوجيه العضو الجديد فوراً لشاشة الانتظار مع تسجيل الطلب ليظهر في شاشة «طلبات الحسابات» للمدير.
   - حذف قسم الأدوار التجريبية من واجهة تسجيل الدخول لاقتصارها على الدخول الحقيقي والتوثيق الرسمي.

2. **إزالة آلية البريد المضمّن للمدير العام (`Master Admin Hardcoded Email Removal — Security Fix`):**
   - حذف البريد المضمّن `see313see@gmail.com` من الكود وقاعدة البيانات نهائياً (كان يمنح صفة مدير تلقائياً لأي من يسجّل به — ثغرة أمنية).
   - الأمان يعتمد كلياً على عمود `role` في جدول `profiles` المحمي بسياسات RLS.
   - كل مستخدم جديد يُنشأ `member/pending` وينتظر موافقة المدير من شاشة طلبات الحسابات.
   - سكربت إصلاح قاعدة البيانات: `supabase/secure_admin_fix.sql`.

3. **التحديث التلقائي الشامل لبيانات التطبيق بمجرد الدخول والفتح (`Automatic App Refresh`):**
   - دمج `WidgetsBindingObserver` في `_MawkibAppState` في `lib/app.dart` لتحديث كافة البيانات تلقائياً فور تشغيل التطبيق وعند العودة إليه من الخلفية (`AppLifecycleState.resumed`).
   - ربط شريط التنقل السفلي في `AppShell` لإلغاء صلاحية الذاكرة المؤقتة للبيانات الخاصة بالتبويب المختار وجلب البيانات الحية فوراً.
   - تزويد شاشات المشتريات والمشتركين بـ `RefreshIndicator` للسحب اليدوي.

4. **تفعيل تفاعلات المنشورات الفورية (الإعجاب والتعليقات والحفظ):**
   - التحديث التفاؤلي الفوري (`Optimistic Instant UI`) في `PostsNotifier` لزيادة ونقصان العدادات بلمسة واحدة.
   - تأمين صور المعلقين في `CommentsBottomSheet` لتوافق الروابط السحابية والملفات المحلية والأصول.

5. **التحصين الأمني الشامل وإغلاق الثغرات (Security Hardening):**
   - توسيع التشفير بـ AES-256 ليشمل صناديق المشتريات (`boxPurchases`) وإدارة المستخدمين (`boxAdminUsers`) مع مسحها الإجباري الفوري عند تسجيل الخروج.
   - حراسة مسارات `appRouter` لمنع الدخول غير المصرح عبر الروابط المباشرة (Deep Links) للشاشات الإدارية.

6. **إصلاح عدادات كروت الحالة في شاشة المشتركين (الكل، المسددون، المتأخرون):**
   - ضبط العدادات لحساب النطاق الدقيق للتبويب المختار حالياً (`شهري` أو `سنوي`) والبحث الفعلي.

7. **تنسيق المبالغ التلقائي بالفوارز (`ThousandsFormatter`):**
   - تفعيل التنسيق التلقائي المباشر بالفوارز لخانة المبالغ عند كتابة مبالغ الاشتراكات والتبرعات.

8. **تطوير شاشة التقارير والتحليلات والمخططات الدائرية المركزية:**
   - مخطط دائري مركزي بحجم 260×260 مع محوّل انزلاقي `CupertinoSlidingSegmentedControl` للتبديل بين الموقف المالي وتوزيع المساهمين.
   - كروت تفاصيل متناسقة وكروت كشف تفصيلي شاملة لكافة الأقسام.

9. **إدارة سجل الزيارات، سجل الطلبات، سجل المحظورين، وسجل الأرشيف المركزي:**
   - توثيق وتفعيل كافة كروت التقارير التفاعلية وربطها بالشاشات الحقيقية والتخزين المحلي المشفر والسحابي.

10. **الخزنة المالية وإدارة المشتريات:**
    - كارت الخزنة المالية الزجاجي الزمردي لحساب الرصيد المتاح، وسجل المشتريات مع طباعة وصولات الصرف عبر `PdfPurchaseService`.

11. **نظام القصص التفاعلي (Instagram Stories Bar):**
    - دعم نشر القصص المتعددة وتجميعها تحت حساب «قصتك» مع شريط التقدم المقسم `Segmented Progress Bar` وزر الحذف الفوري وعرض الصور بأعلى دقة.

12. **إدارة الملف الشخصي (`EditProfilePage`):**
    - إتاحة تغيير الصورة الشخصية وتعديل الاسم ورقم الهاتف وحفظ التغييرات محلياً وسحابياً.

13. **إزالة نافذة البريد الأساسي لمدير النظام (محذوف — كان ثغرة أمنية):**
    - تم حذف بلاطة وحوار "البريد الأساسي لمدير النظام" من صفحة الإعدادات بالكامل ضمن إصلاح الثغرة الأمنية.

15. **إصلاح تسجيل الدخول على الهواتف وتصحيح معالجة وترجمة أخطاء المصادقة (`Mobile Auth & Error Handling Fix`):**
    - إضافة أذونات الإنترنت وحالة الشبكة (`android.permission.INTERNET` و `android.permission.ACCESS_NETWORK_STATE`) في ملف `android/app/src/main/AndroidManifest.xml` لتمكين تطبيقات الهواتف من الاتصال بسحابة Supabase في وضع الـ Release.
    - إعادة بناء دالة `arabicError()` في `supabase_repository.dart` لتشمل كافة رموز وحالات خطأ Supabase (`invalid_credentials`, `invalid_grant`, `user_not_found`, إلخ) وترجمتها بدقة إلى «البريد الإلكتروني أو كلمة المرور غير صحيحة»، ومنع وقوعها في شرط «كلمة المرور ضعيفة» الذي كان يظهر خطأً للمستخدمين.
    - توحيد تهذيب وتصغير أحرف البريد الإلكتروني (`.trim().toLowerCase()`) في `signUp` و `signIn` و `resetPassword` لتفادي تعارض لوحات مفاتيح الهواتف التي تقوم بتكبير أول حرف تلقائياً.
    - إضافة خيارات `autocorrect: false` و `enableSuggestions: false` و `autofillHints` لحقول تسجيل الدخول وإنشاء الحساب.
    - إضافة اختبارات وحدة شاملة في `test/auth_error_test.dart` للتحقق من ترجمة كافة أخطاء المصادقة والشبكة وقواعد البيانات بدقة.

17. **تحديثات الإصدار `1.0.4` (Build 5) — البصمة، أذونات الهواتف، حفظ البيانات، وتحديث الواجهات وإصلاح حظر المستخدمين:**
    - **توحيد رقم الإصدار ديناميكياً:** إضافة `appVersion` و `appBuildNumber` و `versionDisplay` داخل `AppConfig` وربط شاشة الإعدادات وخدمة التحديث التلقائي بها لضمان تغير رقم الإصدار تلقائياً مع كل تحديث.
    - **تفعيل البصمة الحيوية لنظام أندرويد بالكامل:** تحويل `MainActivity.kt` إلى `FlutterFragmentActivity` وإضافة أذونات `USE_BIOMETRIC` و `USE_FINGERPRINT` وضبط `BiometricService` ودعم الدخول السريع بالبصمة في شاشة الدخول.
    - **تذكر بيانات تسجيل الدخول:** حفظ البريد وكلمة المرور محلياً وخيار "تذكر بيانات الدخول" لتعبئة الخلايا تلقائياً لتسهيل تسجيل الدخول.
    - **تفعيل أذونات المعرض والصوتيات والكاميرا:** إضافة كافة أذونات قراءة الوسائط والتخزين والصوت والكاميرا في `AndroidManifest.xml` لأجهزة أندرويد الحديثة والقديمة.
    - **إعادة تصميم واجهة طلبات الحسابات:** كروت طلبات ناعمة متدرجة أبيض بزمردي خفيف تعرض الاسم فقط، مع نافذة تفاصيل زجاجية ضبابية كثيفة (`BackdropFilter`) وسط الشاشة تعرض الاسم الثلاثي، البريد، رقم الهاتف، تاريخ الطلب، وزري القبول والرفض.
    - **إصلاح استخراج البريد ورقم الهاتف:** عمل `upsert` لبيانات الحساب الجديد في جدول `profiles` مع حقول `email` و `phone` وتوفير دمج واسترجاع تلقائي من سجلات التدقيق والمحلي للطلبات القديمة.
    - **إصلاح شاشة حظر المستخدمين:** إعادة تصميم شاشة حظر المستخدمين بكروت زمردية ملكية متدرجة، وحماية قائمة التمرير من التجميد أو التوقف، مع زر إلغاء الحظر/الحظر وحوارات تأكيد سلسة.
    - **بناء وإصدار APK 1.0.4 (Build 5):** تجميع النسخة وتوقيعها ونسخها إلى `releases/mawkib_zahraa_release.apk` و `releases/app-release.apk`.

18. **إطلاق التحديث `1.1.0` (Build 7) — استقرار شاشة حظر المستخدمين، إصلاح الشريط العلوي، وحماية التخطيط (`Banned Users & Desktop Stability Fix`):**
    - **السبب الجذري للمشكلة:** 
    - **الحلول المنفذة:**
      1. تحديث دالة `create()` في `ContributorsRepository` لإرجاع كائن `ContributorModel` الحقيقي من السيرفر بمعرفه السحابي الصحيح، وتنظيف الحقول وتحديث التخزين المحلي.
      2. ربط عملية تسجيل الرصيد والدفعات والتبرعات في `AddContributorDialog` باستخدام المعرّف الحقيقي `savedContributor.id`.
      3. تحسين معالجة الأخطاء وإظهار رسائل الخطأ العربية الشافية بدلاً من الابتلاع الصامت.
      4. تعزيز `AuthRepository` لتنفيذ `upsert` تلقائي للمدير الأساسي في جدول `profiles` برتبة `admin` وحالة `approved` في Supabase عند تسجيل الدخول أو جلب الملف الشخصي.

21. **نظام إدارة الصلاحيات الشامل ولوحة التحكم التفاعلية (`App Permissions & Granular Access Control`):**
    - **الهدف والمتطلبات:**
      1. توفير لوحة تحكم كاملة للمدير العام بتبويبين تفاعليين: (تبويب صلاحيات التطبيق + تبويب أدوار المستخدمين).
      2. إتاحة تفعيل أو إلغاء كل عملية على حدة (إضافة مشترك، إضافة متبرع، إضافة داعم، عرض أسماء المشتركين، عرض أسماء المتبرعين، عرض أسماء الداعمين، وكافة كارتات وسجلات التقارير الـ 12).
      3. السماح بفتح نوافذ الإضافة لكافة الأعضاء، مع فحص الصلاحية عند الحفظ وإظهار نافذة تنبيه: «تنبيه الصلاحيات: هذه الصلاحية مخصصة لمدير النظام فقط».
      4. في صفحة التقارير: إتاحة دخول الصفحة لكافة المسجلين المعتمدين مع إظهار الملخص المالي العلوي والرسم البياني التفاعلي دائماً، وحجب شبكة كروت الكشوفات المتبقية لغير المصرح لهم.
      5. في المنشورات: قصر النشر على المدير والناشر، ونشر الناشر باسمه وصورته دون علامة التوثيق، وإخفاء أزرار الإضافة والأرشيف والملف الشخصي من شريط المنشورات للأعضاء مع إبقاء أيقونة الإعجابات والتفاعلات للجميع.
    - **الملفات المستحدثة والمعدلة:**
      - `lib/core/config/app_permissions_model.dart` (نموذج الصلاحيات).
      - `lib/core/storage/settings_store.dart` و `lib/core/providers/app_providers.dart` (`appPermissionsProvider`).
      - `lib/features/settings/presentation/user_roles_page.dart` (إعادة بناء شاملة بنظام التبويبات والمفاتيح الفورية).
      - `lib/features/contributors/presentation/widgets/add_contributor_dialog.dart` (فحص الصلاحية عند الحفظ ونافذة التنبيه).
      - `lib/features/reports/presentation/reports_page.dart` (فتح الترويسة والرسم البياني وتصفية الكروت).
      - `lib/features/posts/presentation/posts_page.dart` و `lib/features/posts/data/posts_repository.dart` (أزرار الشريط وتوثيق الناشر).

22. **إطلاق الإصدار 1.2.0 (Build 10) وتفعيل التحديث التلقائي الفوري:**
    - تم بناء حزمة الأندرويد الرسمية `mawkib_zahraa_release.apk` وتحديث ملف `update_config.json` بالإصدار 10 ورفعها على مستودع GitHub.
    - بمجرد فتح التطبيق لدى المستخدمين تظهر نافذة التحديث الإجباري السلسة مع شريط تقدم التنزيل والتثبيت الذاتي بضغطة زر واحدة.

23. **إصلاح تشغيل مثبت أندرويد وإضافة إذن تثبيت الحزم (`APK Auto-Installer & REQUEST_INSTALL_PACKAGES`):**
    - **المشكلة:** عند اكتمال تنزيل التحديث 100%، كان مثبت أندرويد لا يعمل تلقائياً أو يعلق الزر بسبب غياب إذن `REQUEST_INSTALL_PACKAGES` في `AndroidManifest.xml` على هواتف أندرويد الحديثة.
    - **الحل:**
      1. إضافة إذن `<uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>` في `AndroidManifest.xml`.
      2. تحسين آلية التثبيت واستخدام المجلد المؤقت النظيف وحفظ الملف مع `flush: true`.
      3. إضافة زر احتياطي مباشر «تحميل مباشر عبر المتصفح 🌐» يظهر فوراً في حال رفض النظام فتح المثبت أو واجه الهاتف قيوداً أمنية.
      4. توضيح نصوص الأزرار وحالة التقدم («تثبيت التحديث الآن 🚀» بدلاً من النصوص المربكة).

25. **إصدار التحديث الأمني 1.2.2 (Build 12) — تعزيز الأمان والدفاع السيبراني الشامل:**
    - **التحسينات الأمنية المنفذة:**
      1. **تأمين تحديثات APK (`Host Whitelisting`):** إضافة فحص دقيق لرابط تحميل الحزمة لمنع هجمات التحويل الخارجي، وقصر النطاق على مستودع GitHub الرسمي للموكب.
      2. **تشفير التخزين المحلي AES-256:** نقل بريد المدير العام والملف الشخصي المخزن محلياً إلى الصندوق المشفر بعتاد الجهاز (`boxAdminUsers`) لمنع التلاعب في الأجهزة المروّتة.
      3. **تحصين تدفقات تسجيل الخروج:** ضمان مسح البيانات الحساسة من المخزن المشفر والعادي فوراً عند انتهاء الجلسة أو تسجيل الخروج.
      4. بناء حزمة APK المحدثة وتحديث `update_config.json` إلى الإصدار 12 لإرسال التحديث الأمني لجميع الهواتف.

26. **الإصلاحات الشاملة: أمان الملف التفصيلي، إصلاح الدعم العيني، ومحرك التقارير السنوية والشهرية المطوّر:**
    - **1. قصر الملف التفصيلي وجدول السداد وعمليات الحذف على مدير النظام فقط:**
      - حماية مسارات `/subscriber_detail` و `/subscriber_profile` في راوتر التطبيق `AppRouter._guard` وإرجاع غير المدير للصفحة الرئيسية تلقائياً.
      - منع النقر على بطاقات المساهمين في `ContributorTile` لغير المدير مع إظهار تنبيه توضيحي أسفل الشاشة.
      - حظر الدخول المباشر داخل `SubscriberDetailPage` و `SubscriberProfilePage` عبر واجهة تنبيه أنيقة (`صلاحية غير متوفرة: الدخول إلى الملف التفصيلي وجدول السداد متاح لمدير النظام فقط`).
      - إخفاء أزرار الحذف والتعديل وتخصيصها لمدير النظام حصراً.
    - **2. إصلاح خطأ إضافة الدعم العيني (`in_kind enum`):**
      - **السبب:** قاعدة بيانات PostgreSQL القديمة كانت تحتوي على قيمتي `('subscriber', 'donor')` فقط ولم يتم تحديث الـ enum.
      - **الحل الجذري:** 
        1. إنشاء سكريبت الهجرة السحابي `supabase/fix_in_kind_enum.sql` (`ALTER TYPE contributor_type ADD VALUE IF NOT EXISTS 'in_kind';`) وتحديث `supabase/schema.sql`.
        2. إضافة آلية Fallback مرنة وتلقائية في `ContributorsRepository.create` و `update` لاكتشاف خطأ الـ enum وحفظه كـ `donor` مع وسم `[داعم عيني]` تلقائياً لضمان عدم توقف واجهة التطبيق عن الحفظ نهائياً على أي سيرفر أو قاعدة بيانات.
        3. تحسين `ContributorModel.fromJson` لتمييز الداعم العيني واستعادته فورياً.
    - **3. تطوير محرك التقارير الشهرية والسنوية وطباعة A4 (`PdfReportService & Reports Engine`):**
      - إضافة دوال استخراج الدفعات السنوية والشهرية المجمعة في `ContributorsRepository` (`loadYearlyPaymentsForYear`, `loadMonthlyPaymentsForMonth`, `loadYearlyDonationsForYear`).
      - تحديث `PdfReportService` و `ReportDetailsPage` لدعم التقارير السنوية (`isAnnual = true`) والشهرية:
        - **التقرير السنوي:** يعرض لكل مشترك مجموع ما سدده خلال كامل السنة المالية مع خانة «مجموع دفعات السنة»، وللمتبرع مجموع تبرعاته بالسنة، وللداعم وصف مواده العينية.
        - **التقرير الشهري:** يعرض حالة السداد التفصيلية لكل مشترك في الشهر المختار (مسدد / متأخر) ومبلغ اشتراكه، مع وصف المواد العينية للشهر.
        - **تخصيص الكروت:** يطبع التقرير المفتوح (مشتركين فقط، أو متبرعين فقط، أو داعمين فقط، أو التقرير الموحد الجامع لكافة الفئات).

27. **اعتماد المنشورات والستوريات الحقيقية رسمياً وإصلاح صلاحيات الستوري للأعضاء (`Official Feed & Stories Permissions`):**
    - إزالة كافة المنشورات والستوريات والتعليقات الوهمية والتجريبية من الكود بالكامل والاعتماد كلياً على خادم Supabase مع التخزين المحلي النظيف.
    - قصر خيار رفع الستوريز ومشاركة الاستوري وتعديل وحذف المنشورات على المصرح لهم (`canPublish` / `isAdmin`)، وإخفاء الأزرار المضللة عن الأعضاء العاديين.

28. **إصلاح حلقة التحديث المتكرر (`App Update Infinite Loop Fix`):**
    - مزامنة `AppConfig.appBuildNumber` و `AppConfig.appVersion` مع `pubspec.yaml` و `update_config.json`.

29. **إصلاح إلغاء التسديد ومزامنة الرصيد وجدول الدفعات بدقة (`Cancel Payment & Balance Deduction Fix`):**
    - تعديل دالة `saveMonthPayment` في `ContributorsRepository` لتنفيذ عملية `delete()` فورية لسجلات الدفعات الخاصة بالشهر والسنة المحددة من جدول `payments` في Supabase عند الإلغاء أو التعديل.
    - يقوم مشغل قاعدة البيانات `sync_contributor_payments()` بإعادة تجميع الرصيد وتحديث `total_paid` و `last_payment_at` على السيرفر فور الحذف.
    - إعادة حساب `total_paid` المحلي وتحديثه في Hive (`_demoBox` و `boxContributors`) ليعكس الخصم فوراً.

---

## ٨. الجودة ومعايير التطوير

- **فحص الكود المستمر:** تشغيل `flutter analyze` بعد كل تعديل والحرص على خلو المشروع التام من أي أخطاء أو تحذيرات (`No issues found!`).
- **الاختبارات الآلية:** تشغيل `flutter test` والتحقق من اجتياز 100% من الاختبارات بنجاح.
- **التوثيق الدائم:** تحديث هذا الملف بعد كل ميزة أو إصلاح أو قرار هندسي ليكون المرجع الأساسي للمشروع.


