# مسودة نتائج الفحص الأمني — صفحة الإعدادات ونظام الصلاحيات

## نقاط قوية (جيد جداً)
- RLS مفعّل على كل الجداول
- دوال SQL SECURITY DEFINER تتجاوز RLS لكن مع is_approved/can_read_names داخلها
- سياسة profiles_update_self تمنع رفع الدور أو الاعتماد الذاتي
- get_stats يعيد أرقام مجمعة فقط
- التشفير المحلي AES بمفتاح من Keystore/Keychain
- التعليقات تمنع الجلسات المجهولة

## الثغرات المكتشفة

### ثغرة حرجة (P1): تسريب البيانات من الذاكرة المؤقتة المحلية بعد انتهاء الصلاحية
- `SupabaseRepository.fetchList/fetchOne`: عند أي فشل شبكة → يعيد من Hive دون تحقق من الدور الحالي أو الجلسة
- لا يوجد purge تلقائي عند: تسجيل الخروج، تغيير الدور (الترقية/التخفيض)، الحظر، انتهاء الصلاحية (الموافقة)
- HiveService.clearCache() يدوي فقط من صفحة الإعدادات
- البيانات المخزنة: أسماء المتبرعين والمشتركين + سجل الدفعات (ledger_<contributorId>_<year>) + التبرعات + الإحصائيات — بيانات مالية حساسة جداً
- السيناريو: مستخدم مالي/مدير يسجل الخروج → المستخدم التالي (زائر أو عضو) على نفس الجهاز قد يرى قوائم أسماء ومبالغ من الذاكرة إذا فشل الاتصال أو قبل إعادة المزامنة

### ثغرة مهمة (P2): لا يوجد حارس توجيه (route guard)
- `/settings` مسار عادي بدون redirect/refresh
- أي مستخدم قد يصل مباشرة لمسار دون التحقق الدوري من حالة الجلسة (isBanned/isPending/isApproved)
- لكن RLS يمنع البيانات الحقيقية — الخطر عرضي فقط (عرض البيانات المخزنة)

### ثغرة (P2): دوال SECURITY DEFINER تُبنى على my_role() الذي لا يتحقق من is_approved في بعض الحالات؟
- my_role(): يرجع الدور حتى لو غير معتمد (pending/banned) — لكن كل الدوال المستخدمة (is_admin, can_read_names, can_publish) تتحقق من status='approved' ✓ سليم
- لكن RolePermissions.canSeeStats في Dart = true دائماً بينما على الخادم يتحقق is_approved — فرق بين الطبقتين لكن لا خطر مباشر

### ثغرة منخفضة (P3): صندوق boxOutbox غير مُمسح عند مسح الذاكرة المؤقتة
- clearCache() لا يمسح boxOutbox (طابور المزامنة المرفوضة) — طابور قد يحوي بيانات حساسة متبقية

### ثغرة منخفضة (P4): is_anon_session تقرأ JWT مباشرة (auth.jwt()) وهذا مسموح لكنه لا يمنع التلاعب بـ metadata من عميل
- ملاحظة: المستخدم المجهول يستطيع تعديل full_name في metadata لكن handle_new_user لا ينشئ له profile

## توصيات الإصلاح
1. إضافة purgeCacheOnAuthChange: عند signOut → clearCache + outbox
2. عند تحميل الملف الشخصي: إذا كان الدور المتغير مختلفاً عن آخر مزامنة → مسح الصناديق الحساسة
3. قبل عرض البيانات المخزنة: تحقق session.isApproved && صلاحية الدور الحالية
4. إضافة redirect guard في app_router يفحص refresh/session
5. مسح boxOutbox مع clearCache
6. إعادة التحقق الدوري (polling) لدور المستخدم خاصة بعد إجراءات إدارية
